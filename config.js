/* ---------------------------------------------------------------
   SUPABASE CONFIG  —  this is the only file you need to edit.

   1. Go to https://supabase.com/dashboard  ->  New project
   2. SQL Editor -> New query -> paste the contents of schema.sql
      (in this repo) -> Run
   3. Project settings -> API
   4. Copy the "Project URL" and the "anon public" key into the
      object below, then save this file.

   Until you do that, the quiz page will show a setup screen
   instead of the game.
   --------------------------------------------------------------- */

window.MQ_SUPABASE_CONFIG = {
  url: "PASTE_PROJECT_URL_HERE",
  anonKey: "PASTE_ANON_PUBLIC_KEY_HERE"
};
